language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t shanvel26/docker-react -f Dockerfile.dev

script:
  - docker run -e CI=true shanvel26/docker-react npm run test -- --coverage
